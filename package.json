{
  "name": "waza",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "clean": "pnpm -r clean",
    "desktop:dev": "pnpm -F @waza/desktop dev",
    "desktop:electron": "pnpm -F @waza/desktop electron",
    "desktop:build": "pnpm -F @waza/desktop build",
    "desktop:package": "pnpm -F @waza/desktop package"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@typescript-eslint/eslint-plugin": "^7.0.0",
    "@typescript-eslint/parser": "^7.0.0",
    "eslint": "^8.57.0",
    "prettier": "^3.2.0",
    "vitest": "^1.4.0"
  }
}
